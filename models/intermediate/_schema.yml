version: 2

models:
  - name: int_sales_fact
    columns:
      - name: sales_order_number
        tests: [not_null]
      - name: order_amount
        tests:
          - percent_nulls_under_threshold:
              column_name: order_amount
              max_pct: 0.01
